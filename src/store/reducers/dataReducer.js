// import * as ActionTypes from "../actionTypes";

const initialState = {
  count: 0,
};

export const DataReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
